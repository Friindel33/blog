<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title class="header">Contact Us</title>
  </head>
  <body>
    <div class="conteiner">
      <div class="row">
        <div class="col">
          <h1>Contact Us</h1>
          <%= form_for :contact, url: contacts_path do |f| %>

          <% if @contact && @contact.errors.any? %>
            <div class="error_explanation">
              <ul>
                <% @contact.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <p>
            <%= f.label :email %><br/>
            <%= f.text_field :email, style: 'width:50%' %>
          </p>

          <p>
            <%= f.label :message %><br/>
            <%= f.text_area :message, :rows => 5, style: 'width:50%;' %>
          </p>

          <p>
            <%= f.submit :'Send Message', class: 'btn btn-success my-2 my-lg-0' %>
          </p>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
